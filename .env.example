# Configure the Main Agent OpenAI provider in DSH Web UI -> Settings -> Models.
# Native Codex authentication remains authoritative for the Codex subagent.
# Never commit real credentials.
OPENAI_API_KEY=
